clearAgendaArray();
<% @pinpoints.each do |p| %>
updateAgendaArray('<%=p.id%>','<%=p.time.strftime("%I:%M %p")%>','<%=p.place%>',
  '<%=p.latitude%>', '<%=p.longitude%>')
<% end %>

rewriteAgenda();
setTimeout(direction_update_all, 20);