//When a creation failed, add error messages to error message area.
(function(){
  var error_messages = "<%= escape_javascript(render 'shared/error_messages', object: @pinpoint) %>";
  $('.new-pinpoint-error').html(error_messages);
})();